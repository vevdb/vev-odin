// Package vev provides an Odin interface to the VevDB embedded database.
//
// It loads the VevDB C ABI dynamically, verifies the ABI version, and wraps
// native connection and string ownership in explicit Odin procedures.
package vev
